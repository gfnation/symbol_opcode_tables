#include "opcodeTable.h"

Tree buildOpcodeTable(FILE *op)
{
  char ins[instruction];
  int code, form;
  Tree *left_node;
  Tree *right_node;

  /*while(!(feof(op)))
  {
    fscanf("%s %d %d", ins, &code, &form);

  }*/
  
}